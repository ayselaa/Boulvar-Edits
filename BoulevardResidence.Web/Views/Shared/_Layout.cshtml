@using Microsoft.Extensions.Localization
@inject IStringLocalizer<BoulevardResidence.Web.Resources.SharedResource> sl;
@{
    //var home = sl["key_home"].Value;
    //var welcome = sl["key_welcome"].Value;
}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="format-detection" content="telephone=no" />
    <meta name="theme-color" content="#282828" />
    <title>BOULEVARD RESIDENCE WEBSITE</title>
    <meta name="author" content="Bulvar Residence" />
    <meta name="description" content="Bulvar Residence" />
    <meta name="keywords" content="Bulvar Residence" />

    <!-- SOCIAL MEDIA META -->
    <meta property="og:description" content="Bulvar Residence | Website" />
    <meta property="og:image" content="#" />
    <meta property="og:site_name" content="bulvarresience" />
    <meta property="og:title" content="bulvarresience" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="#" />

    <!-- TWITTER META -->
    <meta name="twitter:card" content="Bulvar Residence" />
    <meta name="twitter:site" content="@@bulvarresience" />
    <meta name="twitter:creator" content="@@bulvarresience" />
    <meta name="twitter:title" content="Bulvar Residence" />
    <meta name="twitter:description" content="Bulvar Residence" />
    <meta name="twitter:image" content="Bulvar Residence" />

    <!--  Slick Slider Imports Start-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/jquery.slick/1.6.0/slick.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.5.8/slick.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.5.8/slick-theme.min.css">
    <!--  Slick Slider Imports End-->
    <!--  Bootstrap Css Imports Start   -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous" />
    <!--  Bootstrap Css Imports End   -->
    <!-- MDB -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.1/mdb.min.css" rel="stylesheet" />

    <!--Font Awesome Imports Start   -->
    <script src="https://kit.fontawesome.com/e0f20ffb07.js" crossorigin="anonymous"></script>
    <!--Font Awesome Imports End   -->
    <!--CSS File Imports Start   -->
    <link rel="stylesheet" href="~/assets/css/apartments.css">
    <link rel="stylesheet" href="~/assets/css/style.css" />
    <link rel="stylesheet" href="~/assets/css/responsive.css" />
    <link rel="stylesheet" href="~/assets/css/animation.css" />

    <!--CSS File Imports End   -->
    <!--Map Api Start-->
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyD57wRcrfko48s1TmFWjhT5TH9fTMgLzOY"></script>
    <script src="https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/markerclusterer.js"></script>
    <!--Map Api End-->
    <!--noui slider-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/13.1.1/nouislider.min.css">
</head>

<body>
    <!--Header Start-->
    @await Component.InvokeAsync("Header")
    <!--Header End-->
    @RenderBody()

    @*    <div class="header__box-lang">
    <form id="language" asp-controller="Language" asp-action="OnPostLang">
    <select name="lang">
    <option value="az-AZ">az</option>
    <option value="ru-RU">ru</option>
    <option value="en-US">en</option>
    </select>
    </form>
    </div>*@


    <!-- Footer Start-->
    @await Component.InvokeAsync("Footer")
    <!-- Footer End-->

    <script src="https://code.jquery.com/jquery-1.11.0.min.js"></script>

    <!-- Slick Slider -->
    <script src="https://cdn.jsdelivr.net/jquery.slick/1.6.0/slick.min.js"></script>

    <!-- MDB -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.1/mdb.min.js"></script>
    <!--no ui Slider import-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/13.1.1/nouislider.min.js"></script>

    <!-- Script Js -->
    <script src="~/assets/js/script.js"></script>
    <script src="~/assets/js/map.js"></script>
    <script src="~/assets/js/filterprice.js"></script>

    <script src="https://cdn.jsdelivr.net/npm/@@popperjs/core@2.9.2/dist/umd/popper.min.js"
            integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p"
            crossorigin="anonymous"></script>

    <!-- Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"
            integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF"
            crossorigin="anonymous"></script>



    @*  <script>
    let lang = "en-US";

    let langs = document.querySelectorAll("#language select");

    langs.forEach(changeLanguage);

    function changeLanguage(e) {
    e.addEventListener("change", function() {
    fetch(`/lang/${e.value}`, {

    // Adding method type
    method: "GET",
    // Adding headers to the request
    headers: {
    "Content-type": "application/json; charset=UTF-8"
    }
    })
    // Converting to JSON
    .then(response => checkResponse(response))
    // Displaying results to console

    });
    }

    function checkResponse(response) {
    if (response.ok) {
    location.reload();
    }
    }

    </script>*@

    @*  <script>
    // Dil tercihini çerezden al
    let lang = getCookie("userLanguage");

    let langs = document.querySelectorAll("#language select");

    langs.forEach(changeLanguage);

    function changeLanguage(e) {
    e.addEventListener("change", function() {
    // Seçilen dili çerezde kaydet
    setCookie("userLanguage", e.value, 365);

    fetch(`/lang/${e.value}`, {
    // Diğer kodlar buraya gelecek
    })
    .then(response => checkResponse(response));
    });
    }

    function checkResponse(response) {
    if (response.ok) {
    location.reload();
    }
    }

    // Çerez değerini almak için kullanılan fonksiyon
    function getCookie(name) {
    const value = `; ${document.cookie}`;
    const parts = value.split(`; ${name}=`);
    if (parts.length === 2) return parts.pop().split(';').shift();
    }

    // Çerez değeri eklemek/güncellemek için kullanılan fonksiyon
    function setCookie(name, value, days) {
    const date = new Date();
    date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
    const expires = `expires=${date.toUTCString()}`;
    document.cookie = `${name}=${value}; ${expires}; path=/`;
    }

    </script>*@

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
